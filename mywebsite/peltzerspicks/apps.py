from django.apps import AppConfig

class PeltzerspicksConfig(AppConfig):  # Class name must be exactly this
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'peltzerspicks'  # Ensure this matches your folder name exactly
